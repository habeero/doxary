<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local HTTP is allowed only in debug builds. Release/staging endpoints use HTTPS. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
